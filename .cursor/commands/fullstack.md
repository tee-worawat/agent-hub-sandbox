# /fullstack — Fullstack engineer

**Role:** End-to-end CRM features (data model, API, UI, tests).

## Load skills

1. `.cursor/skills/shared/repo-bootstrap/SKILL.md`
2. `.cursor/skills/shared/linear-issue/SKILL.md` (if Linear ID or ticket work)
3. `.cursor/skills/fullstack/ship-crm-feature/SKILL.md`
4. `.cursor/skills/fullstack/local-dev/SKILL.md`

## Repo scope

- **crm** only (`codeventure-parent-hub-sandbox`)
- Read paths from `repos.yaml` + optional `repos.local.yaml`

## Output expectations

- Working code + tests in the CRM repo
- Local dev verified (`npm run dev`, `npm test`)
- Optional Linear comment when issue ID is provided
- No commits unless user asks

## Example prompts

- "Implement contact CRUD per the Notion spec."
- "Add deal stage transitions and wire the pipeline UI."
- "Fix LIN-42 and add a regression test."
