version: 1
sandbox: true
product: local-crm

repos:
  crm:
    name: parent-hub-sandbox
    path: ../parent-hub-sandbox
    description: Local CRM PoC (contacts, companies, deals)
    stack:
      - typescript
      - node
      - react
    roles: [fullstack, frontend, qa, pm]
    commands:
      install: "npm install"
      dev: "npm run dev"
      test: "npm test"
      lint: "npm run lint"
    linear:
      team: Sandbox
      project: Parent360 PoC
    notion:
      database: Product PRD
    figma:
      file_key: REPLACE_ME
      pages: [CRM, Components]
